"""Stage 2+ modular surfaces.

Each subpackage here is a self-contained module that registers with the host
via its services seam. Stage 2 introduces:
  - purchase_interface  — reusable purchase/settlement machine (see PURCHASE_INTERFACE_SPEC.md)
  - shop                — Salesman section + 'product' listing handler
"""
